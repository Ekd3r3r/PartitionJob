---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: partitionjobs.webapp.my.domain
spec:
  group: webapp.my.domain
  names:
    kind: PartitionJob
    listKind: PartitionJobList
    plural: partitionjobs
    singular: partitionjob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PartitionJob is the Schema for the partitionjobs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PartitionJobSpec defines the desired state of PartitionJob
            properties:
              partitions:
                description: Partitions define the number of pods being upgraded to
                  the new version
                format: int64
                type: integer
              replicas:
                description: Replicas define the number of pod replicas to be created
                format: int64
                type: integer
              upgradeStrategy:
                description: UpgradeStrategy describes how the job will be handled.
                  Only one of the following upgrade strategies may be specified. If
                  none of the following startegies is specified, the default one is
                  UpgradeAll.
                enum:
                - All
                - PartitionOnly
                type: string
            type: object
          status:
            description: PartitionJobStatus defines the observed state of PartitionJob
            properties:
              activePartitions:
                description: ActivePartition define the number of pod replicas currently
                  upgraded to the new version
                format: int64
                type: integer
              activeReplicas:
                description: ActiveReplicas define the number of pod replicas currently
                  active
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
